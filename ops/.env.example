# Canonical production env file (single source of truth)
# Copy to ops/.env and fill real secrets.

# Runtime
NODE_ENV=production
PORT=3000
NEXT_PUBLIC_SITE_URL=https://forestbar.pl
HOME_PAGE_SLUG=home

# Database (canonical)
DATABASE_URL=postgres://forestcatering:CHANGE_ME@127.0.0.1:5432/forestcatering
# Backward compatibility alias used by some scripts/code paths
DATABASE_URI=postgres://forestcatering:CHANGE_ME@127.0.0.1:5432/forestcatering

POSTGRES_DB=forestcatering
POSTGRES_USER=forestcatering
POSTGRES_PASSWORD=CHANGE_ME
POSTGRES_HOST=127.0.0.1
POSTGRES_PORT=5432

# Payload
PAYLOAD_SECRET=CHANGE_ME_MIN_32_CHARS
PAYLOAD_REVALIDATE_SECRET=CHANGE_ME_MIN_32_CHARS
PAYLOAD_PREVIEW_SECRET=CHANGE_ME_MIN_32_CHARS

# SMTP (optional)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=kontakt@forestbar.pl
ADMIN_EMAIL=admin@forestbar.pl
