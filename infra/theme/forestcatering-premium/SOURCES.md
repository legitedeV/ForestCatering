# Źródła zasobów graficznych ForestCatering Premium

Plik jest nadpisywany przez `infra/scripts/theme-assets-install.sh` na podstawie `assets.sources.json`.
Skrypt zawsze pobiera pliki od nowa, wymusza weryfikację SHA-256 i kończy działanie błędem, jeśli hash źródła się nie zgadza.
