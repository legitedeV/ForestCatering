# Public URL to PrestaShop API endpoint (container/internal or local host)
NEXT_PUBLIC_PS_API_URL=http://127.0.0.1:8080

# Optional server-side webservice key for authenticated API calls.
# Keep real value only in local .env, never in git.
PS_WEBSERVICE_KEY=
